# --- MCP endpoint auth -------------------------------------------------------
# When set, every request to /api/mcp must carry the token in one of:
#   Authorization: Bearer <MCP_BEARER_TOKEN>
#   X-API-Key: <MCP_BEARER_TOKEN>
#   cf-aig-authorization: <MCP_BEARER_TOKEN>
# (alternatives exist for connector UIs that reserve the Authorization
# header for OAuth). When unset, the endpoint is public. Set it in Vercel (Project Settings →
# Environment Variables) and put the same token into your MCP client config.
MCP_BEARER_TOKEN=

# --- e-Sbírka ----------------------------------------------------------------
# Registered public REST API key (header esel-api-access-key). Without it the
# client falls back to the keyless SPA gateway.
ESBIRKA_API_KEY=
# The proven host is api.e-sbirka.cz; after deploying, run
# dawmain_probe_sources and pin whichever host answers with your key.
# ESBIRKA_API_BASE=https://api.e-sbirka.cz

# --- EUIPO Guidelines --------------------------------------------------------
# Editions change yearly and each gets a new publication id. Override when
# euipo_guidelines_toc starts erroring about an outdated edition.
# EUIPO_GUIDELINES_TM_PUB=2319054
# EUIPO_GUIDELINES_DESIGN_PUB=2231430

# --- Used by scripts/smoke.mjs ----------------------------------------------
# MCP_URL=https://<deployment>.vercel.app/api/mcp
# SMOKE_LIVE=1 to exercise real upstreams (works only where egress is open)
MCP_URL=
